shader_type canvas_item;

render_mode blend_add;

void fragment() {
    //if (COLOR.a > 0.4) {
        //COLOR = vec4(1.0, 1.0, 1.0, 1.0);
    //} else if (COLOR.a > 0.2) {
		//COLOR = vec4(1.0, 0.0, 0.0, 1.0);
    //} else {
		//discard;
    //}
	COLOR = COLOR;
}
